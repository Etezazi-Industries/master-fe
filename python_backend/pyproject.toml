[project]
name = "python-backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.116.1",
    "loguru>=0.7.3",
    "pyodbc>=5.2.0",
]

[dependency-groups]
dev = [
    "ruff>=0.12.8",
]
